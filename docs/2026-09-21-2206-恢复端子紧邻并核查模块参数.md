# 恢复端子紧邻并核查模块参数

时间：2026-09-21 22:06（Asia/Shanghai）。项目：uolo-panel-studio。

## 用户需求

恢复灰蓝端子紧邻排列；修正 DIN-1DIMU4 通道数，增加 DIN-1DIM4 并官网检查其它模块；修复端子到继电器接线转弯变细。

## 修改与原因

- `src/core/modules.js`：端子紧邻不再跳过固定位置端子。同排连续预留槽位按实际宽度贴合，每段首端子锚定，空槽和其它器件隔断；自动端子仍从首排空位开始。安装槽位预留不变，三维 x 为紧缩后的实际中心。
- `src/view/terminal-studio3d.js`、`src/view/wire-routing.js`：原接线平面 z=3 与背板重叠，导致只显示残余细线；改用原柜内配线 z=38 深度。独立等段采样曲线保证短转角被采样，截面半径沿线一致，并按截面采用原柜内线径公式。
- `src/data/products/`：官网核实 DIN-1DIMU4 为4路，新增4路前沿 DIN-1DIM4；修正 Crestron 尺寸/供电及其它品牌中可查证的类别/尺寸错误。公共工厂允许条目覆盖核实状态，避免占位参数被统一显示为已核实。
- 原有产品 ID 保留；无法证实的型号、缺少版本或 OEM 型号的条目明确待核。详细证据见 `2026-09-21-Crestron官网参数核查.md`、`2026-09-21-其他模块官网参数核查.md`。

## 验证

- 全量测试：27 个文件，129 项通过；包括端子固定槽位紧邻、原位置逻辑、导线每个截面半径、目录通道与端口/预算集成。
- `npm run build` 成功，离线 `dist/index.html` 更新；`git diff --check` 通过。
- 独立浏览器测试方案：两组8节灰端子夹蓝色单端子，实际边界间距为0，截图确认线径完整。截图 `_smoke-terminal-packed-wires.png`，视觉评估95分，通过，结果保存于同目录 JSON；未创建 OMX 状态。
- 浏览器分别安装 DIN-1DIMU4、DIN-1DIM4，界面各显示4路，保存刷新后仍为4路。
- 使用独立自动化浏览器数据，不覆盖用户日常浏览器方案。服务继续运行 `http://localhost:5173`。

## 限制与遗留

- 官网未核实 DIN-4DIMU4 精确型号，旧条目保留且标记待核；其它型号不完整的目录同样明确说明。
- 目录尺寸更新会影响既有方案的三维外形及占位计算，按现有布局规则处理。
- 通用端口是逻辑示意，未重建厂家逐针接线图；厂家散热间距记于产品说明，当前布局未自动强制。
- 浏览器仍有原有 Three.js 环境贴图警告；本次功能验证未遇到运行时异常。
- 未提交、推送或部署远程。
