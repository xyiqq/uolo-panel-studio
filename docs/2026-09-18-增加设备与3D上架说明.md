# 2026-09-18 增加设备与3D上架说明

## 日期
2026-09-18

## 目标与用户授权范围
用户询问：增加设备后图上没有出现，应如何操作。本轮只做只读分析与操作说明，未改代码。

## 环境
本地项目 `uolo-配电箱`（V5 / Studio3D）

## 操作与证据
只读检索 `app.js`（`PX`/`UA`/`de`/`WA`）、`domain.js`（`buildAssembly`）、`studio3d.js`（`uX` 仅渲染 `product && !overflow`）。v5-bridge 不负责加设备。

## 实际改动文件
未修改文件。

## 验证结果
未跑 UI 实测；结论来自代码路径核对。

## 风险与未验证项
智能模块 `width:null`、overflow 是否即用户现场情况未对照其当前方案数据。

## 回滚方式
无改动。

## 下一步
若按标准装入仍不上图：查顶栏「未装入」、柜体容量、该产品 width/modules。
