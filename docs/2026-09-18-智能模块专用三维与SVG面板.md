# 2026-09-18 智能模块专用三维与SVG面板

## 日期
2026-09-18

## 目标与用户授权范围
用户要求智能模块不得用空壳/断路器壳表示，需单独 SVG/模型；可参考官网尺寸与 CAD。本轮：按公开资料补齐 45 款外形，并做参数化 3D + SVG 面板（不入库厂家专有 CAD，避免版权问题）。

## 环境
- Vitest / Vite build / preview `:4175`

## 操作与证据
1. 新增 `smart-device3d.js`：按 kind/品牌绘制壳体、协议徽章、通道灯、电源通风/电表 LCD；`isSmartVisual` 时 `Xo` 走智能外观。
2. `smartFaceSvg` + 文档页「智能模块面板图」。
3. 全目录补 `modules/width/height/depth`：Crestron/Lutron(LQSE 12M)/Mean Well HDR·MDR·NDR/MDT·ABB·涂鸦·DALI PS 等；电气额定仍为待核。
4. `npm test` 23 通过；`npm run build` 成功。

## 实际改动文件
- `src/view/smart-device3d.js`（新）
- `src/view/studio3d.js`（Xo 分支）
- `src/view/documents/smart-faces.js`、`documents/index.js`
- `src/data/products/din-layout.js` 及 crestron/lutron/knx/tuya/meanwell/dali 目录
- `tests/core/smart-catalog.test.js`
- 本记录

## 验证结果
- 45/45 有外形尺寸
- 测试 23/23
- 已重建 dist；需硬刷新后重新装入智能模块查看

## 风险与未验证项
- 部分同族占位（MDT TE、涂鸦 OEM、HQP7 示意）须本型号 Spec 复核，面板已注明
- 未把 STEP/厂商 CAD 导入仓库（版权）；三维为参数化示意
- 未 Playwright 截图智能模块单体（逻辑与目录已测）

## 回滚方式
还原本轮列出的文件并重建。

## 下一步
硬刷新 → 器件库装入 DIN-DALI-2 等 → 箱内应见深色控制模块壳 + 品牌色面板；文档中心可看面板 SVG。
