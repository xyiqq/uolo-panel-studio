# 确认优诺箱体与线槽深度

- 日期：2026-09-23。
- 目标与授权：用户确认优诺箱体深度按 150 mm、线槽深度为 80 mm。本轮只改本地代码，未提交、未推送（用户这次没有要求提 PR）。
- 环境：Windows PowerShell；项目 `C:\Users\zhang\Projects\uolo-panel-studio`，基于 main `96ac09e`。

## 实际改动

| 文件 | 改动 |
|---|---|
| `src/data/cabinets/uolo.js` | `DUCT_STANDARD.ductDepth` 由 60 改为 80（箱体深度保持 150）；删除注释和型号来源说明里的“待核”；新增校验：线槽深度必须大于 0 且小于箱体深度。 |
| `src/view/wire-ducts3d.js` | 缺省线槽深度改为引用 `DUCT_STANDARD.ductDepth`，不再写死 60。 |
| `tests/core/uolo-cabinets.test.js` | 新增：6 个内置型号深度为 150/80、来源说明不含“待核”；线槽深度大于等于箱体深度时给出提示。 |

对话框中“线槽深度”的默认值直接读取 `DUCT_STANDARD`，所以会自动变为 80。

## 验证结果

- `npx vitest run`：58 个文件、351 项全部通过。
- `npm run build` 成功，`dist/index.html` 3,576.83 kB。

## 风险与未验证项

- 之前已经生成并保存在方案里的自定义优诺箱体，保留当时的线槽深度（60），需要重新生成才会变为 80。
- 未在浏览器中目测 80 mm 线槽的效果。

## 回滚

- 把 `ductDepth` 改回 60，并撤销本表中的改动。

## 下一步

- 用户刷新页面确认效果；如需合并到 main，再提 PR。
