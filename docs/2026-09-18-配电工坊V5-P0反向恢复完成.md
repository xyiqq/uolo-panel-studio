# 2026-09-18 · 配电工坊 V5 P0 反向恢复完成

## 日期
2026-09-18

## 目标与用户授权范围
- 用户指令：「开始实施吧！」——按既定 V5 计划执行 **P0**：从 V4 单文件 HTML 恢复可维护的 Vite 工程。
- 本轮范围：工程初始化、领域抽取与语义改名、黄金回归、UI/端子图迁移、3D 占位、单文件构建与冒烟。
- 不在本轮：智能模块扩展（P1）、文档中心（P2）、Docker、真实 Three.js 装配台完整移植。

## 环境
- OS：Windows 10 (26200)
- 工程根：`C:\Users\zhang\uoloproject\uolo-配电箱`
- Node / npm：本地已有；Vite **5.4.11**、Vitest **2.1.8**（因 Vite 8 / Vitest 5 在本机 rolldown 绑定失败而钉死）
- 保留原文件：`配电工坊V4.html`（未覆盖）

## 操作与证据

### 工程与依赖
- `package.json` 固定：`vite@5.4.11`、`vitest@2.1.8`、`vite-plugin-singlefile@2.0.2`、`three`、`lucide`、`jsdom`
- `vite.config.js`：singlefile + 大体积 assets 内联
- 入口：`index.html` → `src/main.js` → `src/ui/app.js`

### 领域与测试
- `src/core/domain.js`：语义改名后的装配/配线核心
- `src/core/legacy-names.js`：UI 兼容短名再导出
- `tests/golden/v4-default.test.js`：**5/5 通过**
  - 默认方案：28 产品、117 负荷、44 回路、48 节点、108/144 模数、496 端口、248 导线、带电 44、错误 0 / 待核 56

### 视图与 UI
- `src/view/terminal-schematic.js`：端子接线图（已去 Three 污染）
- `src/view/studio3d.js`：**Studio3D / Qo 占位**（P0 保证壳不崩；完整 3D 待后续）
- `src/ui/app.js`：V4 UI 包装（lucide + domain legacy + 示意图 + 证据图 + CSS）
- `src/assets/evidence/`：手册证据 PNG + `index.js`
- `src/styles/app.css`

### 构建
```
npm test   → 5 passed
npm run build → dist/index.html 约 5,384.74 kB（gzip ≈ 3,409 kB）
```

### 浏览器冒烟（`npm run preview` → http://127.0.0.1:4173/）
| 检查项 | 结果 |
|--------|------|
| 标题 / 壳 UI | 配电工坊 / 04 正常渲染 |
| 模数占用 | **108 / 144 M** |
| 页脚校核 | **0 项需修正 · 56 项待核** |
| 接线统计 | **248 / 248 连接** |
| 端子接线页 | C13 烤箱独立 · 完整连接 248 条；接线表可用 |
| 装配三维区 | 占位（无 WebGL 机柜），与 stub 预期一致 |

## 实际改动文件（本阶段累计，摘要）
- 新增：`package.json`、`package-lock.json`、`vite.config.js`、`.gitignore`、`index.html`
- 新增：`src/main.js`、`src/ui/app.js`、`src/styles/app.css`
- 新增：`src/core/domain.js`、`src/core/_legacy-domain.js`、`src/core/legacy-names.js`、相关 `data` 再导出
- 新增：`src/view/terminal-schematic.js`、`src/view/studio3d.js`
- 新增：`src/assets/evidence/*`、`tests/golden/v4-default.test.js`
- 新增：`scripts/rename-domain.mjs`、`scripts/wrap-ui.mjs`
- 产物：`dist/index.html`（构建生成）
- 未修改：`配电工坊V4.html`（作为权威源保留）

## 验证结果
- 黄金测试：通过
- 生产构建：通过
- Preview 冒烟：容量 / 校核 / 接线数与黄金一致；端子页可用；三维为占位

## 风险与未验证项
- **Studio3D 未真实移植**：装配设计中心区无机柜三维；截图/交互仅为 stub API
- UI 仍为大体量压缩包装文件，后续需按模块继续拆分以便维护
- 未做：导入导出全路径、撤销栈深度、浅色主题全覆盖、移动端布局专项
- Vite 8 路线暂缓（本机 binding 问题）；升级需另测

## 回滚方式
1. 弃用 `src/` + `dist/`，继续只用 `配电工坊V4.html`
2. 或 `git checkout` / 删除本轮新增工程文件（提交前均为本地可逆）

## 下一步
1. **补完真实 Studio3D**（Three 短名 shim / OrbitControls / 机柜模型）
2. 按计划进入 **P1**：智能模块与多品牌/空间尺寸（参数须有手册依据）
3. **P2**：文档中心（标签+QR、系统图 PDF/SVG）
4. 可选：Docker 包装单文件静态站
