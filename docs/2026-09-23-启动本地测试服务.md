# 启动本地测试服务

- 日期：2026-09-23。
- 目标与授权：用户要求启动服务进行本地测试。
- 环境：Windows PowerShell；项目 `C:\Users\zhang\Projects\uolo-panel-studio`。
- 操作：执行 `npm run dev -- --host 127.0.0.1 --port 5174 --strictPort`，Vite 启动成功。
- 证据：控制台显示 `Local: http://127.0.0.1:5174/`；通过 `Invoke-WebRequest` 请求首页返回 HTTP 200。
- 服务：当前终端会话保持运行，监听 `127.0.0.1:5174`。
- 风险与限制：仅本机访问，未进行浏览器完整交互验收。
- 停止：在运行服务的终端按 `Ctrl+C`。
