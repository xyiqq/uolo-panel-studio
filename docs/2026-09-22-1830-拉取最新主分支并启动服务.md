# 拉取最新主分支并启动服务

- 用户需求：拉取最新版本到本地并启动服务。
- 实际操作：确认原工作区干净，原功能分支已包含在远端主分支中；切换 main 并执行 git pull --ff-only origin main，更新至 35fb08a。
- 最新内容：网络设备、PDU 模型及可选规划工具等上游变更。未手工修改业务代码或配置。
- 依赖：npm ci 成功，锁文件未变更。
- 服务：使用 Node 运行 Vite，在后台监听 http://127.0.0.1:5173/，启动时进程号 18996。
- 关键文件：package.json、package-lock.json、vite.config.js；上游功能涉及 src/ 与 tests/。
- 验证：Vite 启动成功，首页 HTTP 200 且包含应用入口；main 与 origin/main 同步。未执行完整测试或浏览器交互检查。
- 遗留风险：npm ci 报告 5 项依赖漏洞（2 moderate、2 high、1 critical），本次未升级依赖；服务仅供本机访问。
