# 精绘 USMART 网转 RS-485 模块

- 日期：2026-09-23。
- 目标与授权：用户根据实物照片确认 USMART 网转串口模块为 24V 供电、1 个 LAN、2 路或 4 路 RS-485，并要求按实物精绘。
- 操作：更新 USMART 2 路/4 路产品资料；增加 24V DC 控制供电、RS-485 端口名称和端口位置；将 4 路版本绘制为上方 3 个端子、下方 1 个端子；补充深色外壳、LAN 插口、POWER/RUN/DATA 指示灯、SETUP 按键及端子标签。三维装配中 LAN 与 RS-485 端子改为位于设备顶部、插拔方向朝上；网口继续不作为电气接线端子。
- 改动文件：`src/data/products/smart-usmart.js`、`src/view/module-face.js`、`src/core/ports.js`、`tests/core/usmart-meanwell.test.js`、`tests/core/module-wiring.test.js`；新增本记录。未提交或推送。
- 电气边界：24V DC 已按用户说明写入 `powerInput`，功耗、RS-485 针脚、通信距离和厂家额定仍标为待核；不把外观示意当成厂家接线图。
- 验证：定向测试 2 个文件、7 项通过；全量测试 52 个文件、316 项通过；`npm run build` 成功。4 路端子布局和所有 SVG 图元边界通过现有几何测试。
- 风险与回滚：端子孔数和具体 V/A/B/G 针脚仍需实物或厂家端子图确认；如需更改可回退本次 5 个源码/测试文件的修改。当前本地 Vite 服务会通过 HMR 使用新构建代码。
- 后续修正：按用户确认，将面板型号文字从 `IHS-TCP/IP` 改为 `DIN-TCP`，并同步副标题显示。
