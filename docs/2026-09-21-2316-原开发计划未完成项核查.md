# 原开发计划未完成项核查

## 需求与范围
对照《2026-09-18-配电工坊V5整体实施计划》与当前代码，核查后续尚未完成项。本轮仅审查并留档，未修改业务代码或启动新增开发。

## 结论
旧《P1至P4一次性落地》记录不能视为全部验收完成：部分底层模块存在，但入口、渲染或导出链路尚未接通。近期已完成的安装记忆、端子批量连接、线号/线径、模块紧邻、空开馈电、单件/链路拆解不再列为待开发。

| 优先级 | 原任务 | 当前缺口与证据 |
|---|---|---|
| 高 | P1-4/P1-7 完整器件链与配线 | 模块编辑器已有 feed/perChannel/feedCircuitId，domain.buildWiring 未读取这些配置，也未串联 circuits.devices 的 control/meter；当前 module-feeds 扩展主要实现保护器与模块一对一馈电。DC24 电源取电、通信总线导线及线类尚未形成完整网络。见 src/ui/app.js:552、src/core/module-feeds.js、src/core/domain.js。 |
| 高 | P2/P3 施工交付一致性 | 共享模块系统图仍按 circuitId 前缀查找节点，正常 K1/D1 不会完整进入器件链；总线摘要未使用实际预算。见 src/view/system-diagram.js:184、290。 |
| 高 | 新功能与 P3 BOM 联动 | terminalConnections 已有独立文档页和 ZIP CSV，但未纳入 net.wires，因此统一接线表、线材 BOM、号码管不包含该部分。模块馈线继承 externalSection，会被 BOM 当出箱电缆按回路长度算。见 src/core/handover.js:96、src/view/documents/index.js:49、src/view/documents/wiring-table.js:5、src/core/bom.js:142、156。 |
| 高 | P1-8 智能校核 | 总线电源/预算/数量与 DALI 限额已有；计划中的通道额定、模块保护配合、模块缺零、DC 80%预算、SELV 同排隔离、金属箱无线提醒、电子模块漏保提示等规则未完整实现。src/core/audit/smart.js 目前仅实现部分规则。 |
| 中 | P1-5/P1-6 强弱电排分区与空间审计 | 空间统计、箱体推荐和备用比例已有；未提供按导轨逐排指定 power/control 的操作及相应排布约束。SPACE_HEIGHT/SPACE_DEPTH/HEAT_BUDGET 没有按计划成为审计项，虽 computeSpace 已计算高度/深度/热量。 |
| 中 | P1-1/P4-1 方案版本与多方案存储 | schema v3/loadDesign、localStorageAdapter 已有但未被 app/main 使用；主入口仍用 v2 simplified 验证和 panel-studio-v4。缺多方案保存/切换/删除以及真正接通的 v3迁移。src/core/schema/index.js、src/storage/local.js、src/ui/app.js:77、src/data/design-templates.js:31。 |
| 中 | P4-2 在线方案链接 | nginx提供 /d/ 路由回落，但前端没有按pathname的designId读取方案；在线二维码不能算完整可用。Docker配置存在，旧记录明确未实际部署验收；本轮未运行Docker。 |
| 中 | P2 标签排版 / P3 修订签认 | 标签模板与打印入口已有；缺 A4 30格/48格及自定义行列边距。修订目前手动添加，导出/打印未提示修订摘要；签认UI只有姓名，没有日期/备注编辑。src/ui/v5-bridge.js:475、583、649、714、724。 |
| 中 | P1-2/P1-3 产品数据 | 自定义模块可录基本尺寸/协议/通道；每路额定、总线消耗、PSU输出等高级字段未完整开放。部分泛型号缺订货号仍待核：DIN-4DIMU4、QS-Link-PSU、HQP7系列、部分DALI与涂鸦设备。参见本项目9月21日两份官网参数核查。 |
| 后续维护 | P0-3/P0-5 工程拆分 | core/domain.js、ui/app.js、studio3d.js仍有大量压缩遗留实现，legacy-names.js和three-shim保留，未达到计划的完整语义化与拆分要求。功能可运行不代表该工程化任务完成。 |

## 已有、仅待验收
文档中心、SVG、浏览器打印入口、标签PNG/P-touch CSV、二维码生成、检查单、手动修订签认、ZIP、Docker/nginx配置均有实现，不能笼统列为未开发。手机真机扫码、Chrome/Edge实际分页和Docker运行属于补验收；本轮未执行这些测试。

## 建议顺序
1. 修正并打通配线网络 → 统一接线表/系统图 → 线材BOM，防止三维可见而交付材料漏项。
2. 补智能额定与保护校核、强弱电分排。
3. 完成v3与多方案管理、在线链接，再补标签排版和修订流程。

## 不属于原必做范围
直接生成中文PDF、直连标签机、REST后端/云端方案、HA实体导出在原计划第10章仅列可选后续；报价、多用户也明确范围外，不应当作原计划欠交付。

## 核查方法与限制
对照计划任务与当前调用入口、配线/渲染/导出代码；子任务通过只读fixture验证馈线BOM分类问题。本轮不以现有165项测试通过推断未覆盖功能已完成，未新增执行全量测试。诊断结论按conversation-change-log要求留档，未提交或推送。
