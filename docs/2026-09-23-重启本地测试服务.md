# 重启本地测试服务

- 日期：2026-09-23。
- 目标与授权：用户要求启动服务进行测试。仅操作本机开发服务，未改代码、未提交、未部署。
- 环境：Windows PowerShell；项目 `C:\Users\zhang\Projects\uolo-panel-studio`。
- 操作与证据：
  - 5174 端口上原有服务仍在运行：PID 23576，经命令行核对确认是本项目的 Vite。当天修复过程中删除了 PNG、新增 WebP、修改了 `package.json` 和 lock 文件，为避免开发服务器沿用旧的模块缓存，决定重启。
  - 结束 PID 23576 及其启动进程 PID 41448，以隐藏窗口重新执行 `npm run dev -- --host 127.0.0.1 --port 5174 --strictPort`。
  - 日志显示 “Re-optimizing dependencies because lockfile has changed”，`ready in 714 ms`。
- 实际改动文件：仅新增本记录。
- 验证结果：首页 HTTP 200；`/src/assets/evidence/manual.webp` HTTP 200，类型 `image/webp`。当前监听进程 PID 3380，启动进程 PID 35184。
- 日志：`%TEMP%\uolo-panel-studio-vite-5174.log`、`%TEMP%\uolo-panel-studio-vite-5174-error.log`（错误日志为空）。
- 风险与未验证项：只做了 HTTP 检查，未做浏览器交互验收；服务仅监听本机。
- 回滚：无代码改动。停止服务前先核对 PID 3380 仍是本项目 Vite。
- 下一步：用户打开 http://127.0.0.1:5174/ 测试（已打开的页面请强制刷新 Ctrl+F5）。
