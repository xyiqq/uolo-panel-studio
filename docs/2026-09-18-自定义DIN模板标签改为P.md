# 2026-09-18 · 自定义 DIN 尺寸模板标签改为 P

## 日期
2026-09-18

## 目标与用户授权范围
新增品牌/型号对话框中，DIN 尺寸卡片显示为 `1P / 2P / 3P…`，不再用 `1M / 2M / 3M`。

## 环境
本机项目 `uolo-配电箱`

## 操作与证据
修改 `src/data/products/custom-templates.js` 中 `DIN_SIZE_TEMPLATES[].label`（及对应 hint 文案）。内部 `modules` 字段与 `id`（如 `din-3m`）未改，仍按 18 mm 模位计算宽度。

## 实际改动文件
- `src/data/products/custom-templates.js`
- `docs/2026-09-18-自定义DIN模板标签改为P.md`（本文件）

## 验证结果
- `npm test -- tests/core/smart-catalog.test.js`：已执行（见本轮终端输出）。
- UI：刷新后打开「新增品牌 / 型号」应看到 `1P · 18×90×60` 等标签。

## 风险与未验证项
未改产品库缩略图上的 `nM` 占位标注（与本对话框无关）。

## 回滚方式
将 label 中的 `P` 改回 `M`。

## 下一步
无。
