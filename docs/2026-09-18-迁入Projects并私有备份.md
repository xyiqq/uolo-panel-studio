# 2026-09-18 · 迁入 Projects 并私有备份

## 日期
2026-09-18

## 目标与用户授权范围
用户要求把本项目放到 Projects，便于晚上回家继续做。

## 环境
- 源目录：`C:\Users\zhang\uoloproject\uolo-配电箱`
- 目标目录：`C:\Users\zhang\Projects\uolo-panel-studio`
- 账号：GitHub `xyiqq`（私有仓）
- 说明：本机 Windows 暂不支持 Cursor Origin CLI，改用私有 GitHub 备份

## 操作与证据
1. `robocopy` 复制源码到 Projects（排除 `node_modules` / `dist`）
2. `git init` + 首提交 `ff520c5`
3. `gh repo create uolo-panel-studio --private --source=. --remote=origin --push` 成功
4. Agent 工作区已切到 `C:\Users\zhang\Projects\uolo-panel-studio`

## 实际改动文件
- 新建目录 `C:\Users\zhang\Projects\uolo-panel-studio`（完整工作副本）
- `.gitignore` 增加排除本地单文件导出 HTML
- 本记录文件

## 验证结果
- 远程：`https://github.com/xyiqq/uolo-panel-studio`（private）
- `git` 跟踪 `origin/main`
- 未修改原 `uoloproject\uolo-配电箱` 目录内容（仅复制）

## 风险与未验证项
- 原路径与 Projects 副本此后会分叉，回家请以 Projects / GitHub 为准
- Cursor Origin 在线仓未创建（Windows 不支持）

## 回滚方式
删除 `Projects\uolo-panel-studio`；需要时可 `gh repo delete xyiqq/uolo-panel-studio`（须用户明确授权）

## 下一步
回家：`git clone https://github.com/xyiqq/uolo-panel-studio.git` 后 `npm install && npm run dev`
