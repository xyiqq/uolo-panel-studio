# 拉取最新版本并启动本地测试服务

- 日期：2026-09-23。
- 目标与用户授权范围：用户要求拉取最新版本并启动服务以便本地测试；未要求提交、推送或部署远程。
- 环境：Windows PowerShell；项目 `C:\Users\zhang\Projects\uolo-panel-studio`；远程 `origin` 为 `https://github.com/xyiqq/uolo-panel-studio.git`。
- 操作与证据：
  - 执行 `git fetch origin --prune`。
  - 本地 `main` 与 `origin/main` 均为 `276510bc9d15f760bf565dc8dd0937bc76f70b4d`（完善配电箱交付、模块供电与端子布局，并补齐接手文档 #4），已是最新，无需再 merge。
  - 保留工作区未提交改动（`src/core/ports.js`、`src/data/products/smart-usmart.js`、`src/view/module-face.js`、`src/view/smart-device3d.js` 及对应测试）与既有未跟踪 docs，未丢弃、未提交。
  - 确认 `5174` 空闲后，以隐藏窗口启动 `npm run dev -- --host 127.0.0.1 --port 5174 --strictPort`。
- 实际改动文件：仅新增本记录；未改业务源码、未改依赖、未提交、未推送。
- 验证结果：Vite 日志显示 `ready in 615 ms`，`Local: http://127.0.0.1:5174/`；`Invoke-WebRequest` 首页返回 HTTP 200（内容长度 492）。监听进程 OwningProcess `23576`，启动 cmd PID `41448`。
- 服务：`http://127.0.0.1:5174/`；日志 `C:\Users\zhang\AppData\Local\Temp\uolo-panel-studio-vite-5174.log`，错误日志同目录 `uolo-panel-studio-vite-5174-error.log`。
- 风险与未验证项：未做完整浏览器交互与打印验收；当前 Vite 会按工作区源码热更，含未提交的 USMART 相关本地改动。
- 回滚：停止服务前请核对占用 `5174` 的进程仍为本项目 Vite，再结束对应 node/cmd；代码回滚可用 `git stash` 或独立 worktree 查看干净 `276510b`。
- 下一步：用户打开或强制刷新 `http://127.0.0.1:5174/` 进行测试。本记录仅本机 `docs/` 留存。
