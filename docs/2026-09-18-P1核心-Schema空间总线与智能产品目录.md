# 2026-09-18 P1 核心：Schema/空间/总线/智能产品目录

## 日期

2026-09-18

## 目标与用户授权范围

实现配电工坊 V5 的 **P1 核心**（只写新文件 + 必要时最小改动已有文件）：

- Schema v2/v3、migrate、`loadDesign`
- 端子模板、空间计算、总线预算、智能审计
- 六品牌起步产品（参数一律待核）与通用箱体库
- 对应 vitest：`migrate` / `space` / `buses`
- 不破坏黄金测试；不修改 `配电工坊V4.html`；不大改 `src/ui/app.js`

## 环境

- OS：Windows 10/11，PowerShell
- 项目：`C:\Users\zhang\uoloproject\uolo-配电箱`（panel-studio@5.0.0）
- 验证命令：`npm test`（vitest 2.1.8）

## 操作与证据

1. 按《配电工坊V5整体实施计划》第 5 章 / P1-1…P1-8 落地核心模块与数据文件。
2. 智能产品 `width/height/depth/modules` 与电气额定均为 `null`，`availability:"参数待核"`，`source.file:"厂家资料待补"`。
3. 通用箱体 `makeGenericCabinet` 按公式估算，预置 12 规格 × 3 深度 = 36 个。
4. 运行 `npm test`：**8 files / 22 tests 全部通过**（含 `tests/golden/v4-default.test.js`）。

## 实际改动文件

### 新建

- `src/core/schema/v2.js`
- `src/core/schema/v3.js`
- `src/core/schema/migrate.js`
- `src/core/schema/index.js`
- `src/core/ports.js`
- `src/core/space.js`
- `src/core/buses.js`
- `src/core/audit/smart.js`
- `src/data/products/_smart-factory.js`（内部工厂辅助）
- `src/data/products/smart-crestron.js`
- `src/data/products/smart-lutron.js`
- `src/data/products/smart-knx.js`
- `src/data/products/smart-tuya-din.js`
- `src/data/products/psu-meanwell.js`
- `src/data/products/psu-dali.js`
- `src/data/products/smart-index.js`
- `src/data/cabinets/generic.js`
- `src/data/cabinets/index.js`
- `tests/core/migrate.test.js`
- `tests/core/space.test.js`
- `tests/core/buses.test.js`
- 本记录

### 已有文件最小改动（协作/衔接）

- `src/data/cabinets/madek.js`：导出路径指向 `../../core/domain.js`
- `src/core/domain.js`：接入 `SMART_PRODUCTS` 以便 `allProducts` 可见智能目录（黄金测试仍断言 `BUILTIN_PRODUCTS.length===28`，未改基线数字）

## 验证结果

| 项 | 结果 |
|---|---|
| `npm test` | 通过：22/22 |
| 黄金：默认无模块 108/144、248 导线 | 通过 |
| migrate：默认方案 → v3 校验 | 通过 |
| space：108 模位 × spare0.25 → rowsNeeded=6 | 通过 |
| buses：无电源 / 70 设备超限 / 预算超限 | 通过 |
| SMART_PRODUCTS | 45 条，全部 `参数待核` 且 `width===null` |
| GENERIC_CABINETS | 36 个 |

## 风险与未验证项

- 未在浏览器 UI 中验证产品库页签与“空间与箱体”面板（P1 UI 后续）。
- `buildAssembly` / `buildWiring` 尚未完整消费 v3 `devices`/`modules`（P1-4/P1-7）。
- 智能产品电气参数仍待厂家资料回填；当前审计会对未知宽度报 `PRODUCT_WIDTH_UNKNOWN`。
- 并行会话可能另有 `labels`/`bom`/`system-diagram`/`v5-bridge` 等文件，本记录不对其做完成声明。

## 回滚方式

删除本轮新建的 `src/core/schema/**`、`ports.js`、`space.js`、`buses.js`、`audit/smart.js`、`src/data/products/smart-*`、`psu-*`、`_smart-factory.js`、`cabinets/generic.js`、`cabinets/index.js` 与对应测试；将 `domain.js`/`madek.js` 恢复到接入前版本即可回退 P1 核心。

## 下一步

- P1-4 共享模块 CRUD 与回路器件链 UI
- P1-5 `buildAssembly` 分区与自定义箱体编辑
- P1-7 配线扩展（保持默认方案导线 248）
- 产品参数按厂家 PDF 逐条核验后回填（禁止臆造）
