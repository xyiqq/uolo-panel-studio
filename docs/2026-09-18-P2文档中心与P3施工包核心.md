# 2026-09-18 P2 文档中心与 P3 施工包核心

## 日期
2026-09-18

## 目标与用户授权范围
实现 P2 文档中心 + P3 施工包**纯函数核心**（符号库、系统图、标签、二维码、文档页、BOM、ZIP），不改 `配电工坊V4.html`，不大改 `src/ui/app.js`。依赖仅用已装 `qrcode`、`fflate`（MIT）。口径：「条件性方案，非施工合格结论」。

## 环境
- 项目：`C:\Users\zhang\uoloproject\uolo-配电箱`
- Node / npm：本机现有；`vitest run` via `npm test`

## 操作与证据
1. 阅读 `docs/2026-09-18-配电工坊V5整体实施计划.md` P2/P3 规格与 `domain.js` 导出。
2. 新增纯函数模块（见改动文件列表）。
3. 新增单测：`tests/core/labels.test.js`、`tests/view/system-diagram.test.js`、`tests/core/bom.test.js`。
4. 执行 `npm test`。
5. 冒烟脚本：默认方案系统图 **3** 页、免责水印存在、248 导线两端 tag 有值、BOM items/wires 米数 >0、`buildDocumentPack` 13 页、ZIP 含 README。

## 实际改动文件
### 新增
- `src/view/symbols.js`
- `src/view/system-diagram.js`
- `src/view/qr.js`
- `src/core/labels.js`
- `src/core/bom.js`
- `src/core/pack.js`
- `src/view/documents/_util.js`
- `src/view/documents/cover.js`
- `src/view/documents/circuit-table.js`
- `src/view/documents/door-chart.js`
- `src/view/documents/labels-sheet.js`
- `src/view/documents/wire-tags.js`
- `src/view/documents/nameplate.js`
- `src/view/documents/wiring-table.js`
- `src/view/documents/checklist.js`
- `src/view/documents/audit-list.js`
- `src/view/documents/bom-page.js`
- `src/view/documents/index.js`
- `tests/core/labels.test.js`
- `tests/view/system-diagram.test.js`
- `tests/core/bom.test.js`
- `docs/2026-09-18-P2文档中心与P3施工包核心.md`（本记录）

### 未修改
- `配电工坊V4.html`
- `src/ui/app.js`（文档中心 UI 页签未接，本轮仅核心）

## 验证结果
| 项 | 结果 |
|---|---|
| `tests/core/labels.test.js` | 通过 |
| `tests/view/system-diagram.test.js` | 通过 |
| `tests/core/bom.test.js` | 通过 |
| `tests/golden/v4-default.test.js` 等既有 | 通过（除 buses） |
| `tests/core/buses.test.js` | **4 失败**（`(products \|\| []).map is not a function`，`auditSmart`/`computeBusBudgets` 入参形态问题；本轮未改 `buses.js` / `audit/smart.js`，判定为既有失败） |
| 冒烟：系统图页数 | 3（44 回路 / 16 列分页） |
| 冒烟：无 `undefined` 字样 | 是 |
| 冒烟：ZIP + README 口径 | 是 |

`npm test` 汇总：`Test Files 1 failed \| 7 passed`；`Tests 4 failed \| 18 passed`。本轮新增 3 个测试文件均通过。

## 风险与未验证项
- 文档中心页签 / 打印 CSS / `window.print` 未接入 UI。
- 系统图点击列 `selectCircuit`、标签 PNG、P-touch CSV 导出未做。
- 二维码未做真机扫描验证。
- BOM 线长为估算公式，非实测；价格列未填。
- `buses.test.js` 既有失败未在本轮修复。

## 回滚方式
删除本记录所列新增文件即可；无对既有业务文件的破坏性改动。

## 下一步
1. UI「文档中心」页签接入 `buildDocumentPack` 预览与打印。
2. 交底包一键导出调用 `buildHandoverZip`。
3. 排查并修复 `buses.test.js` / `auditSmart` 产品列表入参。
