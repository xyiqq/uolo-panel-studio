# 2026-09-18 · 配电工坊 V5 P1–P4 一次性落地

## 日期
2026-09-18

## 目标与用户授权范围
- 用户指令：「你接下来所有都一次性做掉」——在 P0 已完成基础上，继续完成 Studio3D 真迁移、P1 智能模块/空间、P2 文档中心、P3 施工包、P4 Docker。
- 约束：不编造产品参数；口径「条件性方案，非施工合格结论」；单文件 HTML + Docker；不做 HA/报价/云。

## 环境
- Windows 10，Node/npm，Vite 5.4.11，Vitest 2.1.8
- 新增依赖：`qrcode`、`fflate`（MIT）

## 操作与证据

### Studio3D
- 自 V4 抽取 `zo`…`Qo` 至 `src/view/studio3d.js`
- `src/view/three-shim.js` 映射压缩短名 → npm `three` + OrbitControls
- 构建成功；IDE 内置浏览器无 WebGL 时显示既有降级文案（真实机柜需本机 Chrome/Edge）

### P1
- Schema：`src/core/schema/{v2,v3,migrate,index}.js`（v2→v3）
- 空间：`src/core/space.js`；总线：`src/core/buses.js`；智能审计：`src/core/audit/smart.js`；端子：`src/core/ports.js`
- 智能产品目录：`src/data/products/smart-index.js`（约 45 条，**全部 width:null / 参数待核**）
- 通用箱体：`src/data/cabinets/generic.js` + `index.js`
- `domain.allProducts` 合并智能目录；`BUILTIN_PRODUCTS` 仍为 28（黄金不变）

### P2 / P3
- 系统图：`src/view/system-diagram.js` + `symbols.js`
- 标签 / QR：`src/core/labels.js`、`src/view/qr.js`
- 文档包：`src/view/documents/index.js`
- BOM / ZIP：`src/core/bom.js`、`src/core/pack.js`
- UI 桥：`src/ui/v5-bridge.js`（空间面板、文档中心页签、交底 ZIP）

### P4
- 存储：`src/storage/local.js`（v4→v5 key 迁移）
- Docker：`docker/Dockerfile`、`nginx.conf`、`docker-compose.yml`（8080，`/d/` 回落）

### UI 桥接说明
- `src/ui/v5-bridge.js` 在 `ra()`（全量重绘）开头挂载，注入「空间与箱体」面板与「文档中心」页签。
- 先前挂在模块末尾/微任务时因打包作用域未执行；已改为 `ra()` 内逗号表达式，冒烟可见文档中心与箱体推荐。

## 验证结果（更新）
- `npm test`：22 passed
- `npm run build`：`dist/index.html` ≈ 5.89 MB
- Preview 冒烟：文档中心页签、空间面板（108 模位 / 推荐箱体）可见；三维在 Cursor 内置浏览器无 WebGL（预期）

## 实际改动文件（摘要）
- 新增：`src/view/three-shim.js`、完整 `studio3d.js`、schema/space/buses/ports/labels/bom/pack、smart 产品与 generic 箱体、documents/system-diagram/qr/symbols、`ui/v5-bridge.js`、`storage/*`、`docker/*`、`README.md`、多项 `tests/core|view/*`
- 修改：`src/core/domain.js`（引入 SMART_PRODUCTS）、`src/ui/app.js`（`__PANEL_STATE__`）、`src/main.js`、`package.json`、`src/data/cabinets/madek.js` 路径
- 未改：`配电工坊V4.html`

## 风险与未验证项
- 共享模块 CRUD / 器件链改装配与配线的完整 UI 流程未做深度交互验收（数据模型与审计已就绪）
- 智能产品全部「参数待核」，装入后会触发宽度未知类提示，属预期
- Docker 镜像未在本机实际 `docker compose up`（仅提供配置；需本机 Docker）
- 文档中心打印分页以 Chrome/Edge 为准，未做全浏览器矩阵
- IDE 浏览器无 WebGL，三维需在外部浏览器验证

## 回滚
- 删除本轮新增 `src/core/schema|space|buses|…`、`src/ui/v5-bridge.js`、`docker/`，恢复 `main.js` 仅 import app；或回退到仅用 `配电工坊V4.html`

## 下一步（可选深化）
1. 本机 Chrome 打开 `dist/index.html` 验证三维机柜与文档打印 PDF
2. `docker compose -f docker/docker-compose.yml up --build` 验收托管
3. 补产品手册来源后填入真实宽度/额定，消除 `PRODUCT_WIDTH_UNKNOWN`
4. 共享模块通道分配与 `buildWiring` 串接的完整 UI
