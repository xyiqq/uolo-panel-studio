# 新增 UOLO 线槽配电箱

- 日期：2026-09-23。
- 目标与授权：用户要求参照 `C:\Users\zhang\uoloproject\uolo-panel-studio\0A-UOLO东边套（主图2024.04.20）.dwg`（及用户导出的同名 `(1).dxf`）里的配电箱，在不影响现有功能的前提下，把箱内线槽加进项目的配电箱尺寸模型。未提交、未部署。代码改在正在运行的 `C:\Users\zhang\Projects\uolo-panel-studio`；`uoloproject` 下的项目副本及图纸只读，未修改。

## 图纸测量（证据）

- 工具：ezdxf 1.4.2；DWG 经本机 ODA File Converter 27.1.0 转为临时 DXF（放在系统 TEMP，不动原文件），并与用户导出的 DXF 对照，两者内容一致（1634 个图元）。
- 配电箱画在图块 `配电箱76/93/110/127/145/160` 中：蓝色外框为外形尺寸，灰色内框为内部尺寸，内框里每排一个灰色安装区，导轨线在安装区中间。

| 箱型 | 外形 | 内部 | 排数 | 安装区 | 线槽 |
|---|---|---|---|---|---|
| 63/76 | 720×850 | 630×760 | 4 | 550×140 | 40 |
| 63/93 | 700×1020 | 630×930 | 5 | 550×138 | 40 |
| 63/110 | 700×1190 | 630×1100 | 6 | 550×136.7 | 40 |
| 63/127 | 700×1360 | 630×1270 | 7 | 550×135.7 | 40 |
| 63/145 | 700×1530 | 630×1450 | 8 | 550×136.2 | 40 |
| 63/160 | 720×1690 | 630×1600 | 9 | 550×133.3 | 40 |

- 规律：安装区与内框四周、以及相邻安装区之间的空隙均为 40 mm，即线槽（左右竖槽，顶、底及每两排之间横槽），与现场照片一致。

## 实际改动

| 文件 | 改动 |
|---|---|
| `src/data/cabinets/uolo.js`（新增） | 按上表由公式生成 6 个型号 `UOLO-63x76 … UOLO-63x160`：内部宽 630；排距 = 安装区高 + 40；`topRail` = 40 + 安装区高/2；每排 30P（540 mm ≤ 550 mm 安装区）；新增字段 `wireDucts {width:40, depth:60, zoneWidth:550, zoneHeight}`；`rowClearance` 取安装区高。 |
| `src/data/cabinets/index.js` | `allCabinets` / `findCabinet` 加入 UOLO 系列（排在玛德克之后）。 |
| `src/core/domain.js` | `resolveCabinet` 支持 UOLO 型号。 |
| `src/ui/app.js` | 箱体下拉新增“UOLO 线槽箱”分组。 |
| `src/view/wire-ducts3d.js`（新增） | 按箱体参数计算线槽矩形，并画成开口线槽（底板加两侧槽壁、无盖板，导线可见）。 |
| `src/view/studio3d.js` | 导轨循环后仅在 `box.wireDucts` 存在时画线槽；UOLO 箱体铭牌显示 “UOLO / 63x110”，其他箱体保持 “MATECH / …” 不变。 |
| `tests/core/uolo-cabinets.test.js`（新增） | 6 个型号尺寸与图纸一致；线槽不压导轨安装区；现有箱体无线槽且顺序不变；选用 UOLO 箱能通过校验、装配和空间计算，模块不进入线槽。 |

## 验证结果

- `npx vitest run`：57 个文件、345 项全部通过。
- `npm run build` 成功，`dist/index.html` 3,572.95 kB。
- 浏览器三维检查：打开页面这一步被取消，**未做浏览器目测**。

## 风险与未验证项

- 图纸只有正视尺寸：箱体深度按 150 mm、外形深度按 170 mm、线槽深度按 60 mm 取值，均为**待核**，已写入型号来源说明。
- 安装方式暂标“明装”，待核。
- 现有方案不受影响：玛德克、通用、自定义箱体的数据与三维画法都没有改动；UOLO 型号只有被选中时才生效。空间推荐列表会多出这 6 个候选型号。

## 回滚

- 删除 `src/data/cabinets/uolo.js`、`src/view/wire-ducts3d.js`、`tests/core/uolo-cabinets.test.js`，并撤销 `index.js`、`domain.js`、`app.js`、`studio3d.js` 中带 `UOLO` / `wireDucts` 的改动。

## 下一步

- 用户在 http://127.0.0.1:5174/ 的箱体下拉中选择“UOLO 线槽箱”，检查三维效果。
- 提供实际箱体深度和线槽深度后更新 `uolo.js` 里的 `DEPTH` 和 `DUCT_DEPTH`。
